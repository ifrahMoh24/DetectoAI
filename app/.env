# DetectoAI Environment Configuration
# Backend API URL - change this to your server IP if running on a different machine
# Use your Mac's IP address for iOS simulator (not 127.0.0.1)
API_BASE_URL=http://172.20.10.3:8000
